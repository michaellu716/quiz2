<center><h1 class="lesson">Quiz Lesson Two</h1></center>
<br/>
<div class="booyah-box col-10 offset-1">
	<% @travels.each do |travel| %>
	  <div>
	    <p>@<%= travel.username %></p>
	    <p>Bio: <%= travel.bio %></p>
	    <p>Location: <%= travel.location %></p>
	    <p>Address: <%= travel.address %></p><br />
	  </div>

	  <hr />
	<% end %>
</div>

<div class="col-md-12 text-center">
<ul class="navbar-nav mr-auto mt-2 mt-md-0">
  <li class="nav-item">
    <%= link_to 'Add Vacation Spot', new_travel_path, class: 'btn btn-primary' %>
  </li>
</ul>
</div>